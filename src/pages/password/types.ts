import { HeaderInnerComponent } from '@components/header';
import { CombineFormComponent } from '@modules/profile';
import { MenuComponent } from '@components/menu';

export type PasswordPageProps = {
    header: HeaderInnerComponent,
    combineForm: CombineFormComponent,
    menu: MenuComponent
}
